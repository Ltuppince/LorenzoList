<div class="container">
    <div class="row">
        <form class="categorySelect col-lg-4">
            <select class="uk-select">
                {{#each categories}}
                <option class="categoryBtn" data-id="{{id}}">{{name}}</option>
                {{/each}}
            </select>
        </form>
    </div>

    <div class="row">
        {{#each items}}
        <div class="uk-grid-match col-lg-4 col-md-4 col-sm-6" data-cat="{{dataValues.CategoryId}}" uk-grid>
            <div class="itemCard" data-id="{{dataValues.id}}">
                <div class="uk-card uk-card-default uk-card-hover uk-card-body">
                    <h3 class="uk-card-title">{{dataValues.title}}</h3>
                    <p>${{dataValues.price}}</p>
                    <div class="uk-card-footer">
                        <a href="#" class="uk-button uk-button-text">More Info</a>
                    </div>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>